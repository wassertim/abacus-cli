---
import { type Locale, getTranslations, localePath } from '../i18n';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
const t = getTranslations(locale);
---

<section class="hero">
  <div class="hero-glow"></div>
  <div class="hero-inner container">
    <h1>{t.home.title}</h1>
    <p class="hero-subtitle">{t.home.subtitle}</p>
    <p class="hero-desc">{t.home.description}</p>
    <div class="hero-install">
      <pre><code>npm install -g abacus-cli</code></pre>
    </div>
    <div class="hero-actions">
      <a href={localePath(locale, '/getting-started')} class="btn btn-primary">{t.home.install}</a>
      <a href={localePath(locale, '/docs/')} class="btn btn-outline">{t.home.viewDocs}</a>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--space-3xl) 0;
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .hero-glow {
    position: absolute;
    top: -40%;
    left: 50%;
    transform: translateX(-50%);
    width: 60%;
    height: 100%;
    background: radial-gradient(ellipse at center, rgba(99, 102, 241, 0.12) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-inner {
    max-width: 42rem;
    position: relative;
  }

  .hero h1 {
    font-size: 3rem;
    font-family: var(--font-mono);
    margin-bottom: var(--space-md);
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--color-text);
    margin-bottom: var(--space-sm);
  }

  .hero-desc {
    font-size: 1rem;
    color: var(--color-text-muted);
    margin-bottom: var(--space-xl);
  }

  .hero-install {
    margin-bottom: var(--space-xl);
  }

  .hero-install pre {
    display: inline-block;
    padding: var(--space-sm) var(--space-lg);
    font-size: 0.95rem;
    background: var(--color-surface);
    border: 1px solid var(--color-border);
  }

  .hero-actions {
    display: flex;
    gap: var(--space-md);
    justify-content: center;
  }

  .btn {
    display: inline-block;
    padding: 0.6rem 1.5rem;
    border-radius: var(--radius-md);
    font-weight: 600;
    font-size: 0.95rem;
    transition: all 0.15s ease;
  }

  .btn:hover {
    text-decoration: none;
  }

  .btn-primary {
    background: var(--color-primary);
    color: white;
  }

  .btn-primary:hover {
    background: var(--color-primary-hover);
    box-shadow: 0 0 20px rgba(99, 102, 241, 0.3);
  }

  .btn-outline {
    border: 1px solid var(--color-border);
    color: var(--color-text);
  }

  .btn-outline:hover {
    border-color: var(--color-text-muted);
    color: var(--color-text);
    background: var(--color-surface);
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 2rem;
    }

    .hero-subtitle {
      font-size: 1.1rem;
    }
  }
</style>
